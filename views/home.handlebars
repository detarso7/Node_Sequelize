<h1>Usuários Cadastrados:</h1>
<div class="container">
    {{#each users}}
        <div class="item">
            <p>Nome: {{this.name}}</p>
            <p>Ocupação: {{this.occupation}}</p>
        </div>     
    {{/each}}
</div>